// Test generated by RoostGPT for test springMongoDB using AI Type Open AI and AI Model gpt-4

package dev.farhan.movieist;

import org.junit.jupiter.api.Test;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

import static org.mockito.Mockito.*;

@SpringBootApplication
public class MovieistApplication_corsConfigurer_7322b2b140_Test {

    public static void main(String[] args) {
        SpringApplication.run(MovieistApplication.class, args);
    }

    @Bean
    public WebMvcConfigurer corsConfigurer() {
        return new WebMvcConfigurer() {

            @Override
            public void addCorsMappings(CorsRegistry registry) {
                registry.addMapping("/**").allowedOrigins("*").allowedMethods("*").allowedHeaders("*").allowCredentials(false).maxAge(3600);
            }
        };
    }

    @Test
    public void testCorsConfigurer() {
        CorsRegistry registry = mock(CorsRegistry.class);
        MovieistApplication_corsConfigurer_7322b2b140_Test movieistApplication = new MovieistApplication_corsConfigurer_7322b2b140_Test();
        
        movieistApplication.corsConfigurer().addCorsMappings(registry);
        
        verify(registry, times(1)).addMapping("/**");
        verify(registry, times(1)).allowedOrigins("*");
        verify(registry, times(1)).allowedMethods("*");
        verify(registry, times(1)).allowedHeaders("*");
        verify(registry, times(1)).allowCredentials(false);
        verify(registry, times(1)).maxAge(3600);
    }

    @Test
    public void testCorsConfigurer_NullRegistry() {
        CorsRegistry registry = null;
        MovieistApplication_corsConfigurer_7322b2b140_Test movieistApplication = new MovieistApplication_corsConfigurer_7322b2b140_Test();
        
        try {
            movieistApplication.corsConfigurer().addCorsMappings(registry);
        } catch (NullPointerException e) {
            assert(e.getMessage().contains("Registry cannot be null"));
        }
    }
}
